name: Hello World
on: [push]
jobs:
  Hello-World-Job:
    env:
      DOTNET_SYSTEM_GLOBALIZATION_INVARIANT: 1
    runs-on:
      - codebuild-gh-runner-test-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - run: echo "Hello World"
      - run: docker buildx version
